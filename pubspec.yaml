name: mad_project2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Updated to recommended stable range

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase Core Packages (Updated to latest major versions)
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6
  firebase_storage: ^11.6.0 # ADDED: Required for image upload
  intl: ^0.18.1

  # firebase_messaging: ^16.0.4

  # Utility Packages
  image_picker: ^1.1.0
  http: ^1.2.0
  cupertino_icons: ^1.0.8
  geolocator: ^11.0.0
  firebase_messaging: ^14.7.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1 # Updated to latest version

flutter:
  uses-material-design: true